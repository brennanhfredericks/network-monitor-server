# Use python base image
FROM python:3.8

# Set the working directory to /app
WORKDIR /app

# Copy the current directory
COPY . /app
ENV DEBUG=true
# install python package
RUN pip install -r requirements_no_wsgi.txt

# run the command to start uWSGI
CMD ["python3","run.py"]
